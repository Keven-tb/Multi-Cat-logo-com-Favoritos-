import './PageLayout.css';

const FavoritosPage = () => {
  //const { favorites, toggleFavorite, isFavorite } = useFavorites();

  return (
    <p>Não há Favoritos ainda</p>
  );
};

export default FavoritosPage;